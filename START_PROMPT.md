# 复制给 Codex 的启动提示词

这是维护者生成 GitHub Release 文案时使用的模板。发布时把第一行替换为该版本对应的完整 commit 地址；XHS 用户应复制 Release 说明中的成品提示词，而不是复制这个仍含占位符的模板。

```text
你现在要协助我完成一次可复核的求职流程。

项目仓库（必须是固定 commit 的 `/tree/<40位commit>` 地址）：<替换为你的 GitHub 固定版本地址>
ego(lite) 官方安装说明：https://lite.ego.app/document/zh/docs/quick-start

请严格按以下顺序工作：

一、加载项目并做安全检查
1. 先确认项目仓库 URL 固定到完整 commit；如果只是 main/master/HEAD、短分支或无法核对的重定向，立即停止并让我提供固定版本。用只读 GitHub 访问或临时浅克隆读取该 commit，并验证实际 commit 完全一致。只加载该版本的 README.md、policies/、config/skills.lock.yaml、tests/acceptance-report.md，以及 `skills/find-my-dream-job/SKILL.md`、`skills/resume-evidence-profile/SKILL.md`、`skills/evidence-job-match/SKILL.md`、`skills/truthful-application-materials/SKILL.md`；不要执行仓库脚本，不要猜测缺失文件的行为。
2. 先告诉我将要访问的网站、需要读取的数据、会产生的文件和可能的外部副作用。
3. 默认只使用上述四个 bundled skill 和已由 ego(lite) 安装的 `ego-browser`。不要重新审计或安装 lock 中的第三方候选；只有 bundled 能力明确不足且我主动选择扩展时，才检查对应来源、固定版本、许可证、脚本/MCP、联网和数据上传行为，并等待我的明确同意。
4. 在公开仓库和临时 clone 之外选择或创建本地私有目录，显示绝对路径并验证它不在任何版本控制目录中。真实简历、联系方式、截图、草稿和 ledger 只能写入该目录；读取仓库不等于授权安装 skill 或上传个人数据。
5. 读取 acceptance report 的发布状态。只要真实 Fill/Submit 的动态门槛尚未全部通过，就禁止在真实招聘方页面填写、上传或提交；只能做 Discover、Draft 或受控测试表单演练。

二、确认 ego(lite) 前置条件
1. 检查 ego(lite) 是否可用。没有安装时，只打开官方安装说明并指导我自行安装；不要下载来路不明的应用，不要复制或导出浏览器密码、Cookie、Token。
2. 我必须亲自在 ego(lite) 中完成 onboarding 和目标网站登录。登录失效、验证码、二次验证或异常页面交给我处理。
3. 在读取简历前说明：哪些简历字段会进入当前 Codex 会话/模型上下文，已知的数据处理方、区域和留存信息，以及未知项；让我选择专用私有输出目录。路径不得位于公开仓库或其他版本控制目录中，进度消息和记录中的本地路径也要脱敏。
4. 验证 `ego-browser` skill 已加载且可调用，并与 `config/skills.lock.yaml` 中锁定的 ego(lite) 版本、skill 版本和 SKILL.md SHA256 完全一致。未加载或不匹配时停止所有网页任务；未加载只按 ego(lite) 官方 onboarding 指引处理，版本变化则先重新审查并发布新的固定仓库版本，不得改用来源不明的浏览器脚本替代。

三、先进入 Discover（发现）模式
1. 向我询问并确认：简历文件、目标城市/远程偏好、岗位方向和关键词、应届全职/实习/专项计划、毕业届别、经验/学历限制、薪资范围、公司偏好、可接受的工作地点、最少匹配条件，以及本次允许访问的网站。不得把“2026届”自行扩展为实习或专项计划。
2. 读取简历，建立候选人事实画像；缺失信息标记为“未知”，不得编造。
3. 使用 ego(lite) 在我允许的网站发现岗位。开始前说明登录态访问会向网站暴露账号、查询词和访问历史，并逐站确认关键词、页数/结果数和时间预算。可以并行搜索，但必须遵守网站条款、频率限制和页面提示，不得绕过验证码、登录保护、反自动化措施或付费墙。
4. 输出去重后的岗位表：公司、岗位、地点、要求、薪资（如有）、原始链接、来源网站、抓取时间、过期/不确定标记和匹配初筛结果。此阶段只读，不改简历、不填表、不提交。

四、我确认后进入 Draft（起草）模式
1. 对我选中的岗位给出匹配分、事实证据、硬性缺口、可通过表达改善的部分和不能弥补的部分；不要把关键词数量当作唯一评分。
2. 针对每个岗位生成材料草稿。所有内容必须来自原简历或我确认的事实；新增数字、职责、技能、证书、雇主和时间线一律标为待确认，未确认不得写入最终材料。
3. 展示修改前后差异、使用的岗位版本和需要我补充的字段。未经我确认，不要覆盖原简历，不要上传材料。
4. 批准草稿只代表认可本地文案，不代表同意向任何网站披露。请生成一条可复制的 `批准草稿：<jobId>/<材料哈希>` 确认语句，不要把“可以”“继续”等模糊回复当作 Fill 授权。

五、我逐个确认后进入 Fill（填写）模式
1. 每次只处理一个岗位。填写或上传本身就会向招聘方披露个人信息，所以先展示公司、岗位、完整最终 URL、材料版本、表单字段与答案、附件哈希、表单结构哈希和可能的协议/隐私影响。搜索偏好默认只用于筛选，不能自动变成表单答案。
2. 把上述快照持久化为 disclosure binding，并生成一条可复制的 `授权填写：<jobId>/<bindingHash>`。只有我原样回复这条独立授权后，才可使用 ego(lite) 填写或上传，并验证所有关键字段。批准草稿或“继续”不算披露授权。必须停在最终提交按钮之前。
3. 薪资、签证/工作授权、公民身份、残障、健康、犯罪记录、退伍军人、族裔、性别、年龄、签名、法律声明和协议勾选始终由我亲自填写或确认；即使出现在 disclosure binding 中也不得由 agent 自动填写。
4. 遇到验证码、二次验证、付款、强制协议、异常页面、登录失效或平台禁止自动化时立即暂停并说明原因，不要尝试绕过。交还控制权后恢复时，先检查我或网站是否已经完成提交；无法确定就记录 unknown，不能继续点击。

六、我再次确认后进入 Submit（提交）模式
1. 重新展示公司、岗位、完整最终 URL、页面中的公司/岗位/application id、表单结构哈希、附件名称和哈希、全部问题+字段 ID+答案以及当前页面状态；Fill 后任何内容变化都会使之前的授权失效。
2. 先生成单次 attemptId 和 submit binding，持久化为 `submit_prepared`，再给我一条可复制的 `确认提交：<jobId>/<attemptId>/<bindingHash>`。只有我原样回复后，才先持久化 `submit_started`，然后点击一次最终提交。任何重启看到 `submit_started` 都直接转 unknown 并人工对账，禁止再次点击。
3. 记录成功、失败、跳过或待人工处理的状态、时间、原始链接、材料版本、两次确认引用、失败原因和后续动作。记录优先保存在本机，不要写入公开仓库或发送到陌生服务。

七、每一步都执行墨菲定律检查
- 检查岗位链接、公司和岗位是否对应，去除重复和已过期信息。
- 检查地点、学历、经验、薪资和到岗时间等硬条件，未知项不能当作满足。
- 检查生成材料是否出现虚构、夸大、错别字、错误联系方式、错误公司名或错误岗位名。
- 检查投递前的简历版本、附件、表单答案和收件地址；发现任一不一致就暂停。
- 保留原始简历和过程记录，任何失败都能恢复，不能因重试造成重复投递。

默认模式是 Discover。除非我明确切换到 Draft、Fill 或 Submit，否则不要修改材料、填写表单或提交申请。填写/上传前取得数据披露授权，最终提交前取得第二次授权；任何安装依赖和不可逆操作也必须先取得我的明确同意。
```
